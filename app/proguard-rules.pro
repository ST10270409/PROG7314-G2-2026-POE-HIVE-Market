# Add project specific ProGuard rules here.
# For a prototype milestone, release minification is off (see app/build.gradle.kts),
# so this file has no active rules yet. Fill in keep-rules here before enabling
# isMinifyEnabled for a real release build — in particular for kotlinx.serialization
# data classes and Retrofit interfaces, which reflection-based tools can strip
# incorrectly by default.
